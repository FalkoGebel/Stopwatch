<UserControl x:Class="StopwatchUiWpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StopwatchUiWpf.Views"
             xmlns:viewModels="clr-namespace:StopwatchUiWpf.ViewModels"
             xmlns:p="clr-namespace:StopwatchUiWpf.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:MainViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="b2v" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding CurrentTime}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding RunningStopTime}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Column="2">
            <TextBlock Text="{Binding SplitTimes}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ScrollViewer>
        <TextBlock Text="{Binding FinalStopTime}" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Button Content="{x:Static p:Resources.MainView_ResetButton_Content}" Grid.Row="1" Visibility="{Binding Path=ResetButtonVisible, Converter={StaticResource b2v}}"/>
        <Button Content="{x:Static p:Resources.MainView_StartButton_Content}" Grid.Column="1" Grid.Row="1" Visibility="{Binding Path=StartButtonVisible, Converter={StaticResource b2v}}" Command="{Binding StartStopProcessCommand}"/>
        <Button Content="{x:Static p:Resources.MainView_PauseButton_Content}" Grid.Column="1" Grid.Row="1" Visibility="{Binding Path=PauseButtonVisible, Converter={StaticResource b2v}}" Command="{Binding PauseStopProcessCommand}"/>
        <Button Content="{x:Static p:Resources.MainView_SplitTimeButton_Content}" Grid.Column="2" Grid.Row="1" Visibility="{Binding Path=SplitTimeButtonVisible, Converter={StaticResource b2v}}" Command="{Binding StopSplitTimeCommand}"/>
        <Button Content="{x:Static p:Resources.MainView_StopButton_Content}" Grid.Column="3" Grid.Row="1" Visibility="{Binding Path=StopButtonVisible, Converter={StaticResource b2v}}" Command="{Binding StopStopProcessCommand}"/>
    </Grid>
</UserControl>
